# AGENTS.md

## Git
- For this repo, treat pushes as dual-remote by default.
- Before any push, run `git remote -v` and confirm both remotes are configured.
- Push the same branch to both repos, not just `origin`.
- If only one remote is configured, stop and ask for or add the missing remote before pushing.
